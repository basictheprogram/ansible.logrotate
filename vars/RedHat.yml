---
# Distribution-default global options for /etc/logrotate.conf on Red Hat / Rocky / Alma.
# Used when logrotate_options is empty.
logrotate_distribution_options:
  - weekly
  - rotate 4
  - create
  - dateext
