---
# Ansible collection requirements for ansible-role-logrotate
#
# Install with: ansible-galaxy collection install -r requirements.yml
#
# community.general is required for SUSE/openSUSE support
# (community.general.zypper module).

collections:
  - name: community.general
    version: ">=9.0.0"
